<!DOCTYPE html>
<html lang="it">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Preventivo - Alberto Seminerio</title>
<link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@600&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
<style>
  :root{
    --bg: #12100e;
    --panel: #1c1815;
    --gold: #c9a24b;
    --gold-light: #e6c877;
    --cream: #f6efe1;
    --line: #3a322a;
    --danger: #b5453f;
    --ok: #4b8a5c;
  }
  *{box-sizing:border-box;}
  body{
    margin:0;
    font-family:'Poppins', sans-serif;
    background: radial-gradient(circle at top, #201b16, #0d0b09 70%);
    color: var(--cream);
    min-height:100vh;
    padding-bottom:60px;
  }
  header{
    padding:22px 16px 14px;
    text-align:center;
    border-bottom:2px solid var(--gold);
  }
  header h1{
    margin:0;
    font-size:1.4rem;
    letter-spacing:1px;
    color: var(--gold-light);
  }
  header p{
    margin:4px 0 0;
    font-size:0.8rem;
    color:#b8ab8f;
  }
  .slots{
    display:flex;
    gap:8px;
    overflow-x:auto;
    padding:14px 12px;
    justify-content:center;
    flex-wrap:wrap;
  }
  .slot-btn{
    border:1px solid var(--line);
    background:var(--panel);
    color:var(--cream);
    padding:8px 14px;
    border-radius:20px;
    font-size:0.85rem;
    cursor:pointer;
    white-space:nowrap;
  }
  .slot-btn.active{
    background: var(--gold);
    color:#1c1815;
    font-weight:600;
    border-color:var(--gold);
  }
  .slot-btn.filled::after{
    content:" •";
    color:#8fbf9a;
  }
  main{
    max-width:640px;
    margin:0 auto;
    padding:0 14px;
  }
  .card{
    background:var(--panel);
    border:1px solid var(--line);
    border-radius:14px;
    padding:18px;
    margin-bottom:16px;
  }
  .card h2{
    margin:0 0 12px;
    font-size:1rem;
    color:var(--gold-light);
    border-bottom:1px solid var(--line);
    padding-bottom:8px;
  }
  label{
    display:block;
    font-size:0.78rem;
    color:#c7b998;
    margin:10px 0 4px;
  }
  input, select, textarea{
    width:100%;
    padding:10px;
    border-radius:8px;
    border:1px solid var(--line);
    background:#141210;
    color:var(--cream);
    font-family:inherit;
    font-size:0.95rem;
  }
  textarea{min-height:80px; resize:vertical;}
  .row2{display:flex; gap:10px;}
  .row2 > div{flex:1;}
  .radio-group{display:flex; gap:16px; margin-top:6px;}
  .radio-group label{
    display:flex;
    align-items:center;
    gap:6px;
    font-size:0.85rem;
    color:var(--cream);
    margin:0;
  }
  .radio-group input{width:auto;}
  .hint{font-size:0.72rem; color:#8a7c60; margin-top:4px;}
  .tipo-multi{
    display:flex;
    flex-direction:column;
    gap:6px;
    background:#141210;
    border:1px solid var(--line);
    border-radius:8px;
    padding:10px;
  }
  .tipo-row{
    display:flex;
    align-items:center;
    gap:8px;
    font-size:0.85rem;
    border-bottom:1px solid var(--line);
    padding-bottom:6px;
  }
  .tipo-row:last-child{border-bottom:none; padding-bottom:0;}
  .tipo-row label{
    flex:1;
    margin:0;
    font-size:0.85rem;
    color:var(--cream);
    display:flex;
    align-items:center;
    gap:6px;
  }
  .tipo-row input[type="checkbox"]{width:auto;}
  .tipo-row input[type="number"]{
    width:70px;
    padding:5px 6px;
    font-size:0.78rem;
  }
  .tipo-row .mini-btn{padding:4px 7px; font-size:0.68rem;}
  .hint-row{
    display:flex;
    align-items:center;
    gap:8px;
    flex-wrap:wrap;
    margin-top:8px;
    font-size:0.76rem;
    color:#b8ab8f;
  }
  .hint-row b{color:var(--gold-light);}
  .mini-btn{
    border:1px solid var(--line);
    background:#141210;
    color:var(--cream);
    padding:5px 9px;
    border-radius:6px;
    font-size:0.72rem;
    cursor:pointer;
  }
  .mini-btn-save{border-color:var(--gold); color:var(--gold-light);}
  .saldo-box{
    display:flex;
    justify-content:space-between;
    background:#141210;
    border:1px solid var(--line);
    border-radius:8px;
    padding:10px 12px;
    margin-top:10px;
    font-size:0.9rem;
  }
  .saldo-box b{color: var(--gold-light);}
  .actions{
    display:flex;
    gap:10px;
    flex-wrap:wrap;
    margin-top:14px;
  }
  button.action{
    flex:1;
    min-width:140px;
    padding:12px;
    border-radius:10px;
    border:none;
    font-weight:600;
    font-size:0.9rem;
    cursor:pointer;
  }
  .btn-save{background:var(--gold); color:#1c1815;}
  .btn-pdf{background:#3a5a72; color:var(--cream);}
  .btn-cal{background:#4b6b4f; color:var(--cream);}
  .btn-clear{background:transparent; color:#c77; border:1px solid #6b3a3a !important;}

  /* --- PREVIEW / PREVENTIVO DOCUMENT --- */
  #preview{
    background: var(--cream);
    color:#2a241c;
    border-radius:10px;
    padding:26px 22px 40px;
    position:relative;
    font-family:'Poppins', sans-serif;
  }
  #preview .top-row{
    display:flex;
    justify-content:space-between;
    font-size:0.85rem;
    margin-bottom:14px;
    color:#5a4d38;
  }
  #preview h3{
    text-align:center;
    letter-spacing:3px;
    margin:0 0 18px;
    color:#3a2f1e;
    font-size:1.1rem;
  }
  #preview .field{
    display:flex;
    margin:6px 0;
    font-size:0.9rem;
  }
  #preview .field .lab{
    width:150px;
    color:#7a6b4c;
    flex-shrink:0;
  }
  #preview .field .val{
    font-weight:600;
    color:#2a241c;
  }
  #preview .divider{
    border:none;
    border-top:1px dashed #b8a878;
    margin:14px 0;
  }
  #preview .note-block{
    margin-top:10px;
    font-size:0.85rem;
    white-space:pre-wrap;
    background:#efe6d0;
    padding:10px;
    border-radius:6px;
    min-height:40px;
  }
  #preview .signature{
    text-align:right;
    margin-top:36px;
    font-family:'Dancing Script', cursive;
    font-size:1.7rem;
    color:#2a241c;
  }
  .status-msg{
    text-align:center;
    font-size:0.78rem;
    color:#8fbf9a;
    min-height:16px;
    margin-top:8px;
  }
  .note-app{
    font-size:0.72rem;
    color:#8a7c60;
    text-align:center;
    margin-top:20px;
    line-height:1.5;
    padding:0 10px;
  }
</style>
</head>
<body>

<header>
  <h1>Alberto Seminerio &middot; Musicista &amp; Animatore Eventi</h1>
  <p>Preventivo</p>
</header>

<div class="slots" id="slots"></div>

<main>
  <div class="card">
    <h2>Dati Evento</h2>

    <label>Tipologia Evento (selezione multipla)</label>
    <div class="tipo-multi" id="tipoMultiContainer"></div>
    <button type="button" class="mini-btn mini-btn-save" style="margin-top:8px;" onclick="sommaListinoSelezionati()">Σ Somma prezzi di listino selezionati</button>

    <label>Evento Singolo o Periodico?</label>
    <div class="radio-group">
      <label><input type="radio" name="ricorrenza" value="singolo" checked> Singolo Evento</label>
      <label><input type="radio" name="ricorrenza" value="periodico"> Periodico (es. serate fisse)</label>
    </div>

    <div class="row2">
      <div>
        <label id="labelData">Data Evento</label>
        <input type="date" id="dataEvento">
      </div>
      <div>
        <label>Orario</label>
        <input type="time" id="orarioEvento">
      </div>
    </div>

    <div id="periodicoFields" style="display:none;">
      <div class="row2">
        <div>
          <label>Frequenza</label>
          <select id="frequenza">
            <option value="Settimanale">Settimanale</option>
            <option value="Mensile">Mensile</option>
          </select>
        </div>
        <div>
          <label>Giorno (se settimanale)</label>
          <select id="giornoSettimana">
            <option>Lunedì</option><option>Martedì</option><option>Mercoledì</option>
            <option>Giovedì</option><option>Venerdì</option><option>Sabato</option><option>Domenica</option>
          </select>
        </div>
      </div>
      <label>Data Fine (opzionale)</label>
      <input type="date" id="dataFine">
      <div class="hint">Il campo "Data Evento" sopra indica la data di inizio della serie.</div>
    </div>

    <label>Nominativo Cliente</label>
    <input type="text" id="nominativo" placeholder="Nome e Cognome / Locale">

    <div class="row2">
      <div>
        <label>Telefono</label>
        <input type="tel" id="telefono" placeholder="+39 ...">
      </div>
      <div>
        <label>Email (opzionale)</label>
        <input type="email" id="email" placeholder="mail@esempio.it">
      </div>
    </div>

    <div id="singoloFields">
      <div class="row2">
        <div>
          <label>Costo Evento (€)</label>
          <input type="number" id="costoEvento" min="0" step="1">
        </div>
        <div>
          <label>Acconto (€)</label>
          <input type="number" id="acconto" min="0" step="1" value="0">
        </div>
      </div>
      <div class="saldo-box">
        <span>Restano a saldare</span>
        <b id="saldoDisplay">€ 0</b>
      </div>
    </div>

    <div id="periodicoPrezzo" style="display:none;">
      <label>Prezzo per Evento (€)</label>
      <input type="number" id="prezzoEvento" min="0" step="1">
    </div>

    <label>Servizi Extra</label>
    <textarea id="serviziExtra" placeholder="Servizi aggiuntivi rispetto a quelli richiesti (es. luci extra, ora supplementare, service audio aggiuntivo...)"></textarea>

    <label>Note</label>
    <textarea id="note" placeholder="Dettagli, richieste particolari, orari di montaggio, ecc."></textarea>

    <div class="actions">
      <button class="action btn-save" onclick="salvaPreventivo()">💾 Salva</button>
      <button class="action btn-pdf" onclick="condividiPDF()">📄 Condividi PDF</button>
      <button class="action btn-cal" onclick="salvaCalendario()">📅 Salva su Calendario</button>
    </div>
    <div class="actions">
      <button class="action btn-clear" onclick="svuotaSlot()" style="flex:1;">🗑 Svuota questo preventivo</button>
    </div>
    <div class="status-msg" id="statusMsg"></div>
  </div>

  <div class="card">
    <h2>Anteprima Preventivo</h2>
    <div id="preview">
      <div class="top-row">
        <span>Data: <b id="pvData">--/--/----</b></span>
        <span>Ora: <b id="pvOra">--:--</b></span>
      </div>
      <h3>PREVENTIVO EVENTO</h3>

      <div class="field"><span class="lab">Cliente:</span><span class="val" id="pvNominativo">—</span></div>
      <div class="field"><span class="lab">Contatto:</span><span class="val" id="pvContatto">—</span></div>
      <div class="field"><span class="lab">Tipo evento:</span><span class="val" id="pvTipo">—</span></div>
      <div class="field"><span class="lab">Ricorrenza:</span><span class="val" id="pvRicorrenza">—</span></div>

      <hr class="divider">

      <div id="pvSingoloBlock">
        <div class="field"><span class="lab">Costo evento:</span><span class="val" id="pvCosto">€ 0</span></div>
        <div class="field"><span class="lab">Acconto:</span><span class="val" id="pvAcconto">€ 0</span></div>
        <div class="field"><span class="lab">Restano a saldare:</span><span class="val" id="pvSaldo">€ 0</span></div>
      </div>
      <div id="pvPeriodicoBlock" style="display:none;">
        <div class="field"><span class="lab">Prezzo a evento:</span><span class="val" id="pvPrezzoEv">€ 0</span></div>
      </div>

      <hr class="divider">
      <hr class="divider">
      <div class="field"><span class="lab">Servizi Extra:</span></div>
      <div class="note-block" id="pvServiziExtra">—</div>

      <div class="field" style="margin-top:10px;"><span class="lab">Note:</span></div>
      <div class="note-block" id="pvNote">—</div>

      <div class="signature">Alberto Seminerio</div>
      <div style="text-align:center; margin-top:14px;">
        <a href="https://linktr.ee/Albertoseminerio" target="_blank" style="font-size:0.75rem; color:#7a6b4c;">Tutti i miei servizi: linktr.ee/Albertoseminerio</a>
      </div>
    </div>
  </div>

  <div class="note-app">
    Il pulsante "Salva su Calendario" funziona quando l'app viene aperta qui in chat (usa l'assistente Claude collegato al tuo Google Calendar).<br>
    Una volta pubblicata su Netlify come app standalone, usa "Condividi PDF" e inserisci manualmente l'evento nel calendario.
  </div>
</main>

<script>
const TIPI_EVENTO = ["Il Capone Quiz", "Karaoke", "Piano Bar", "Animazione Bambini", "Animazione Bimbi Matrimonio", "Matrimonio", "Evento Privato", "Altro"];

const PREZZI_DEFAULT = {
  "Il Capone Quiz": 150, "Karaoke": 150, "Piano Bar": 200,
  "Animazione Bambini": 250, "Animazione Bimbi Matrimonio": 150,
  "Matrimonio": 800, "Evento Privato": 300, "Altro": 0
};

let currentSlot = 1;
const STORAGE_PREFIX = "capone_preventivo_";
const LISTINO_KEY = "capone_listino_prezzi";
const LINKTREE_URL = "https://linktr.ee/Albertoseminerio";

function getListino(){
  try{ return JSON.parse(localStorage.getItem(LISTINO_KEY) || '{}'); }
  catch(e){ return {}; }
}
function setListinoPrice(tipo, prezzo){
  const l = getListino();
  l[tipo] = prezzo;
  localStorage.setItem(LISTINO_KEY, JSON.stringify(l));
}
function getPrezzoIndicativo(tipo){
  const l = getListino();
  if(l[tipo] !== undefined && l[tipo] !== null && l[tipo] !== '') return l[tipo];
  return PREZZI_DEFAULT[tipo] !== undefined ? PREZZI_DEFAULT[tipo] : 0;
}

function renderTipoMulti(selezionati){
  const container = document.getElementById('tipoMultiContainer');
  container.innerHTML = '';
  TIPI_EVENTO.forEach((tipo, idx) => {
    const row = document.createElement('div');
    row.className = 'tipo-row';
    const checked = selezionati && selezionati.includes(tipo) ? 'checked' : '';
    row.innerHTML = `
      <label>
        <input type="checkbox" class="tipo-checkbox" value="${tipo}" ${checked}>
        ${tipo}
      </label>
      <input type="number" class="listino-input" id="listino_${idx}" data-tipo="${tipo}" value="${getPrezzoIndicativo(tipo)}">
      <button type="button" class="mini-btn" onclick="salvaListinoInline(${idx})">💾</button>
    `;
    container.appendChild(row);
  });
  container.querySelectorAll('.tipo-checkbox').forEach(cb => cb.addEventListener('change', () => { updatePreview(); }));
}

function salvaListinoInline(idx){
  const input = document.getElementById('listino_' + idx);
  const tipo = input.dataset.tipo;
  const val = input.value;
  if(val === '' || isNaN(val)){
    document.getElementById('statusMsg').textContent = 'Inserisci un prezzo valido prima di salvarlo nel listino.';
    return;
  }
  setListinoPrice(tipo, parseFloat(val));
  document.getElementById('statusMsg').textContent = `Prezzo di "${tipo}" aggiornato nel listino: € ${val}`;
}

function getSelectedTipi(){
  return Array.from(document.querySelectorAll('.tipo-checkbox:checked')).map(cb => cb.value);
}

function sommaListinoSelezionati(){
  const checked = document.querySelectorAll('.tipo-checkbox:checked');
  if(checked.length === 0){
    document.getElementById('statusMsg').textContent = 'Seleziona almeno una tipologia evento.';
    return;
  }
  let totale = 0;
  checked.forEach(cb => {
    const input = document.querySelector(`.listino-input[data-tipo="${cb.value}"]`);
    totale += parseFloat(input.value) || 0;
  });
  const periodico = document.querySelector('input[name="ricorrenza"]:checked').value === 'periodico';
  const targetId = periodico ? 'prezzoEvento' : 'costoEvento';
  document.getElementById(targetId).value = totale;
  updatePreview();
  document.getElementById('statusMsg').textContent = `Totale da listino: € ${totale} (${checked.length} eventi selezionati)`;
}

function emptyPreventivo(){
  return {
    tipiEvento: ["Il Capone Quiz"],
    ricorrenza: "singolo",
    dataEvento: "",
    orarioEvento: "",
    frequenza: "Settimanale",
    giornoSettimana: "Lunedì",
    dataFine: "",
    nominativo: "",
    telefono: "",
    email: "",
    costoEvento: "",
    acconto: 0,
    prezzoEvento: "",
    serviziExtra: "",
    note: ""
  };
}

function getSlotData(n){
  const raw = localStorage.getItem(STORAGE_PREFIX + n);
  return raw ? JSON.parse(raw) : null;
}

function renderSlots(){
  const el = document.getElementById('slots');
  el.innerHTML = '';
  for(let i=1;i<=6;i++){
    const filled = !!getSlotData(i);
    const btn = document.createElement('button');
    btn.className = 'slot-btn' + (i===currentSlot ? ' active':'') + (filled ? ' filled':'');
    btn.textContent = 'Preventivo ' + i;
    btn.onclick = () => { currentSlot = i; loadSlot(i); renderSlots(); };
    el.appendChild(btn);
  }
}

function loadSlot(n){
  const data = getSlotData(n) || emptyPreventivo();
  renderTipoMulti(data.tipiEvento || []);
  document.querySelector(`input[name="ricorrenza"][value="${data.ricorrenza}"]`).checked = true;
  document.getElementById('dataEvento').value = data.dataEvento;
  document.getElementById('orarioEvento').value = data.orarioEvento;
  document.getElementById('frequenza').value = data.frequenza;
  document.getElementById('giornoSettimana').value = data.giornoSettimana;
  document.getElementById('dataFine').value = data.dataFine;
  document.getElementById('nominativo').value = data.nominativo;
  document.getElementById('telefono').value = data.telefono;
  document.getElementById('email').value = data.email;
  document.getElementById('costoEvento').value = data.costoEvento;
  document.getElementById('acconto').value = data.acconto;
  document.getElementById('prezzoEvento').value = data.prezzoEvento;
  document.getElementById('serviziExtra').value = data.serviziExtra || '';
  document.getElementById('note').value = data.note;
  toggleRicorrenzaUI();
  updatePreview();
  document.getElementById('statusMsg').textContent = '';
}

function collectForm(){
  return {
    tipiEvento: getSelectedTipi(),
    ricorrenza: document.querySelector('input[name="ricorrenza"]:checked').value,
    dataEvento: document.getElementById('dataEvento').value,
    orarioEvento: document.getElementById('orarioEvento').value,
    frequenza: document.getElementById('frequenza').value,
    giornoSettimana: document.getElementById('giornoSettimana').value,
    dataFine: document.getElementById('dataFine').value,
    nominativo: document.getElementById('nominativo').value,
    telefono: document.getElementById('telefono').value,
    email: document.getElementById('email').value,
    costoEvento: document.getElementById('costoEvento').value,
    acconto: document.getElementById('acconto').value,
    prezzoEvento: document.getElementById('prezzoEvento').value,
    serviziExtra: document.getElementById('serviziExtra').value,
    note: document.getElementById('note').value
  };
}

function toggleRicorrenzaUI(){
  const val = document.querySelector('input[name="ricorrenza"]:checked').value;
  const periodico = val === 'periodico';
  document.getElementById('periodicoFields').style.display = periodico ? 'block' : 'none';
  document.getElementById('singoloFields').style.display = periodico ? 'none' : 'block';
  document.getElementById('periodicoPrezzo').style.display = periodico ? 'block' : 'none';
  document.getElementById('labelData').textContent = periodico ? 'Data Inizio Serie' : 'Data Evento';
  document.getElementById('pvSingoloBlock').style.display = periodico ? 'none' : 'block';
  document.getElementById('pvPeriodicoBlock').style.display = periodico ? 'block' : 'none';
}

function formatDataIT(iso){
  if(!iso) return '--/--/----';
  const [y,m,d] = iso.split('-');
  return `${d}/${m}/${y}`;
}

function updatePreview(){
  const d = collectForm();
  document.getElementById('pvData').textContent = formatDataIT(d.dataEvento);
  document.getElementById('pvOra').textContent = d.orarioEvento || '--:--';
  document.getElementById('pvNominativo').textContent = d.nominativo || '—';
  const contatto = [d.telefono, d.email].filter(Boolean).join(' · ');
  document.getElementById('pvContatto').textContent = contatto || '—';
  document.getElementById('pvTipo').textContent = d.tipiEvento.length ? d.tipiEvento.join(' + ') : '—';
  if(d.ricorrenza === 'periodico'){
    let ricTxt = `Periodico — ${d.frequenza}`;
    if(d.frequenza === 'Settimanale') ricTxt += `, ogni ${d.giornoSettimana}`;
    if(d.dataFine) ricTxt += ` — fino al ${formatDataIT(d.dataFine)}`;
    document.getElementById('pvRicorrenza').textContent = ricTxt;
    document.getElementById('pvPrezzoEv').textContent = '€ ' + (d.prezzoEvento || 0);
  } else {
    document.getElementById('pvRicorrenza').textContent = 'Singolo Evento';
    const costo = parseFloat(d.costoEvento) || 0;
    const acc = parseFloat(d.acconto) || 0;
    const saldo = Math.max(costo - acc, 0);
    document.getElementById('pvCosto').textContent = '€ ' + costo;
    document.getElementById('pvAcconto').textContent = '€ ' + acc;
    document.getElementById('pvSaldo').textContent = '€ ' + saldo;
    document.getElementById('saldoDisplay').textContent = '€ ' + saldo;
  }
  document.getElementById('pvServiziExtra').textContent = d.serviziExtra || '—';
  document.getElementById('pvNote').textContent = d.note || '—';
}

function salvaPreventivo(){
  const data = collectForm();
  localStorage.setItem(STORAGE_PREFIX + currentSlot, JSON.stringify(data));
  renderSlots();
  document.getElementById('statusMsg').textContent = 'Preventivo ' + currentSlot + ' salvato ✔';
}

function svuotaSlot(){
  if(!confirm('Svuotare il Preventivo ' + currentSlot + '?')) return;
  localStorage.removeItem(STORAGE_PREFIX + currentSlot);
  loadSlot(currentSlot);
  renderSlots();
  document.getElementById('statusMsg').textContent = 'Preventivo ' + currentSlot + ' svuotato';
}

function condividiPDF(){
  const d = collectForm();
  const { jsPDF } = window.jspdf;
  const doc = new jsPDF({unit:'mm', format:'a5'});
  const pageW = doc.internal.pageSize.getWidth();
  let y = 18;

  doc.setFontSize(9);
  doc.setTextColor(90,77,56);
  doc.text(`Data: ${formatDataIT(d.dataEvento)}`, 12, y);
  doc.text(`Ora: ${d.orarioEvento || '--:--'}`, pageW-12, y, {align:'right'});
  y += 10;

  doc.setFontSize(15);
  doc.setTextColor(40,34,25);
  doc.setFont('helvetica','bold');
  doc.text('PREVENTIVO EVENTO', pageW/2, y, {align:'center'});
  y += 10;

  doc.setFont('helvetica','normal');
  doc.setFontSize(11);

  function field(label, value){
    doc.setTextColor(120,105,75);
    doc.text(label, 12, y);
    doc.setTextColor(40,34,25);
    doc.text(String(value || '—'), 55, y);
    y += 7;
  }

  field('Cliente:', d.nominativo);
  field('Contatto:', [d.telefono, d.email].filter(Boolean).join(' / '));
  field('Tipo evento:', d.tipiEvento.length ? d.tipiEvento.join(' + ') : '—');

  if(d.ricorrenza === 'periodico'){
    let ricTxt = `Periodico - ${d.frequenza}`;
    if(d.frequenza === 'Settimanale') ricTxt += `, ogni ${d.giornoSettimana}`;
    if(d.dataFine) ricTxt += ` - fino al ${formatDataIT(d.dataFine)}`;
    field('Ricorrenza:', ricTxt);
    field('Prezzo a evento:', '€ ' + (d.prezzoEvento || 0));
  } else {
    field('Ricorrenza:', 'Singolo Evento');
    const costo = parseFloat(d.costoEvento) || 0;
    const acc = parseFloat(d.acconto) || 0;
    const saldo = Math.max(costo - acc, 0);
    field('Costo evento:', '€ ' + costo);
    field('Acconto:', '€ ' + acc);
    field('Restano a saldare:', '€ ' + saldo);
  }

  y += 3;
  doc.setDrawColor(184,168,120);
  doc.line(12, y, pageW-12, y);
  y += 8;

  doc.setTextColor(120,105,75);
  doc.text('Servizi Extra:', 12, y);
  y += 6;
  doc.setTextColor(40,34,25);
  const extraLines = doc.splitTextToSize(d.serviziExtra || '—', pageW - 24);
  doc.text(extraLines, 12, y);
  y += extraLines.length * 5 + 8;

  doc.setTextColor(120,105,75);
  doc.text('Note:', 12, y);
  y += 6;
  doc.setTextColor(40,34,25);
  const noteLines = doc.splitTextToSize(d.note || '—', pageW - 24);
  doc.text(noteLines, 12, y);
  y += noteLines.length * 5 + 20;

  doc.setFont('times','italic');
  doc.setFontSize(16);
  doc.text('Alberto Seminerio', pageW-15, y, {align:'right'});

  y += 10;
  doc.setFont('helvetica','normal');
  doc.setFontSize(9);
  doc.setTextColor(120,105,75);
  doc.textWithLink('Tutti i miei servizi: linktr.ee/Albertoseminerio', pageW/2, y, {align:'center', url: LINKTREE_URL});

  const fileName = `Preventivo_${(d.nominativo||'cliente').replace(/\s+/g,'_')}.pdf`;
  const blob = doc.output('blob');
  const file = new File([blob], fileName, {type:'application/pdf'});

  if(navigator.canShare && navigator.canShare({files:[file]})){
    navigator.share({
      files:[file],
      title:'Preventivo Evento',
      text:`Preventivo per ${d.nominativo || 'cliente'}`
    }).catch(()=>{ doc.save(fileName); });
  } else {
    doc.save(fileName);
  }
}

function buildRRule(d){
  if(d.ricorrenza !== 'periodico') return null;
  const giorniMap = {"Lunedì":"MO","Martedì":"TU","Mercoledì":"WE","Giovedì":"TH","Venerdì":"FR","Sabato":"SA","Domenica":"SU"};
  let rule = d.frequenza === 'Settimanale' ? 'FREQ=WEEKLY' : 'FREQ=MONTHLY';
  if(d.frequenza === 'Settimanale') rule += `;BYDAY=${giorniMap[d.giornoSettimana]}`;
  if(d.dataFine) rule += `;UNTIL=${d.dataFine.replace(/-/g,'')}T235959Z`;
  return rule;
}

async function salvaCalendario(){
  const d = collectForm();
  if(!d.dataEvento){
    document.getElementById('statusMsg').textContent = 'Inserisci almeno la data evento prima di salvare sul calendario.';
    return;
  }
  document.getElementById('statusMsg').textContent = 'Creazione evento su Google Calendar in corso...';

  const contatto = [d.telefono, d.email].filter(Boolean).join(' / ');
  const rrule = buildRRule(d);
  const prezzoInfo = d.ricorrenza === 'periodico'
    ? `Prezzo a evento: € ${d.prezzoEvento || 0}`
    : `Costo evento: € ${d.costoEvento || 0} | Acconto: € ${d.acconto || 0} | Saldo: € ${Math.max((parseFloat(d.costoEvento)||0)-(parseFloat(d.acconto)||0),0)}`;

  const prompt = `Crea un evento sul mio Google Calendar con questi dati esatti, usando il tool di Google Calendar disponibile:
- Titolo: ${d.tipiEvento.join(' + ') || 'Evento'} - ${d.nominativo || 'Cliente'}
- Data inizio: ${d.dataEvento}${d.orarioEvento ? ' alle ' + d.orarioEvento : ''}
- Durata: 2 ore (se non diversamente specificato)
${rrule ? '- Ricorrenza (RRULE): ' + rrule : '- Evento singolo, non ricorrente'}
- Descrizione evento: Cliente: ${d.nominativo || '-'} | Contatto: ${contatto || '-'} | ${prezzoInfo} | Servizi Extra: ${d.serviziExtra || '-'} | Note: ${d.note || '-'}
Crea direttamente l'evento usando lo strumento del calendario, senza chiedere conferma, poi rispondi solo con "Evento creato" o un messaggio di errore breve.`;

  try{
    const response = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        model: "claude-sonnet-4-6",
        max_tokens: 1000,
        messages: [{ role: "user", content: prompt }],
        mcp_servers: [
          { type: "url", url: "https://calendarmcp.googleapis.com/mcp/v1", name: "google-calendar" }
        ]
      })
    });
    const data = await response.json();
    const toolResults = (data.content || []).filter(b => b.type === "mcp_tool_result");
    const textBlocks = (data.content || []).filter(b => b.type === "text").map(b => b.text).join(' ');
    if(toolResults.length > 0){
      document.getElementById('statusMsg').textContent = 'Evento creato sul calendario ✔';
    } else {
      document.getElementById('statusMsg').textContent = textBlocks || 'Nessuna risposta dal calendario. Riprova.';
    }
  } catch(err){
    document.getElementById('statusMsg').textContent = 'Funzione disponibile solo aprendo l\'app qui in chat con Claude (non su Netlify standalone).';
  }
}

// Event listeners
document.querySelectorAll('input[name="ricorrenza"]').forEach(r => r.addEventListener('change', ()=>{ toggleRicorrenzaUI(); updatePreview(); }));
document.querySelectorAll('#dataEvento,#orarioEvento,#frequenza,#giornoSettimana,#dataFine,#nominativo,#telefono,#email,#costoEvento,#acconto,#prezzoEvento,#serviziExtra,#note')
  .forEach(el => el.addEventListener('input', updatePreview));

// Init
renderSlots();
loadSlot(1);
</script>
</body>
</html>
